The classes in this folder are used to simulate scheduled jobs.

In an enterprise environment, something like TIBCO or SQL Server jobs or Windows Services would perform these tasks.
This application is currently hosted in a shared hosting environment, so these tasks are run
by periodically accessing a webpage.